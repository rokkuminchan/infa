const { default: Base } = require("./common/base");

export default class Hyouka extends Base {
  executedLessonId;
  lessonId;
  teacherId;
  teachingAssistantId;
  lessonRate;
  teacherRate;
  teachingAssistantRate;
  lessonComment;
  teacherComment;
  TeachingAssistantComment;
}
